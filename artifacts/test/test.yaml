apiVersion: v1
kind: Pod
metadata:
  name: test-bash
spec:
  containers:
  - name: alpine
    image: alpine
    command: ["/bin/sh"]
    args: ["-c", "apk add --no-cache curl && while true; do sleep 3600; done"]
